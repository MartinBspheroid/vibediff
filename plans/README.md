# Implementation Plans

Generated by the `improve` skill on 2026-06-17 (against commit `470b165`).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your
row when done.

Goal driving these plans: make VibeDiff a **production-grade product, including
e2e tests** (maintainer's explicit ask).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add CI workflow (test/lint/build gate) | P1 | S | — | DONE (2026-06-17) — `.github/workflows/ci.yml`: frontend (lint/test/build), backend (vet/`test -race`/golangci-lint), and e2e (Playwright vs built binary) jobs on push+PR. |
| 002  | Fix arbitrary file read / path traversal | P1 | S | 001 | DONE (2026-06-17) — `internal/git/path.go` + `cat`→`os.ReadFile`; regression test in `service_test.go` |
| 003  | Backend Go unit + integration tests | P1 | M | 001 | DONE (2026-06-17) — git 80.6%, review 96.6% cover; parser/service/store/handlers `_test.go` pass under `-race` |
| 004  | End-to-end test harness (Playwright) | P1 | M | 001 | DONE (2026-06-17) — `web/e2e/` Playwright harness drives the real binary vs a temp repo; 4 journeys pass (render, type-switch, add+persist comment, delete). Caught a real `useDiff` loading regression. CI `e2e` job pending plan 001. |
| 005  | Fix frontend async hazards (WS leak, fetch race, silent error) | P2 | M | 001 | DONE (2026-06-17) — hook fixes + tests; `useComments.error` now surfaced as a non-blocking banner in `DiffViewer`. |
| 006  | Server hardening (body limit, validation, error sanitization, WS origin) | P2 | S | 001, 002 | DONE (2026-06-17) — `MaxBytesReader`+`DisallowUnknownFields`+`validateComment`; sanitized error responses; same-origin WS `CheckOrigin`; `MaxHeaderBytes`. Tests in `handlers_test.go`. |
| 007  | DX & docs polish (deps, README, CLAUDE.md, .env.example) | P3 | S | — | DONE (2026-06-17) — `npm audit fix` → 0 vulns; README documents `-no-open` + env vars; added `CLAUDE.md` and `.env.example`. |
| 008  | UI diff-target selector (compare against branch/tag) | P2 | M | — | DONE (2026-06-17) — ported from the `WatchtowerSecurity:diff-since` fork. `GetRefs`/`IsValidRef`/`GetDiffForTarget` + `/api/refs` + validated `?target=` on `/api/diff`; `useRefs`/`TargetSelector` + threaded through `useDiff`. Tests: backend (refs/target/injection-reject), `TargetSelector`/`useRefs` units, e2e journey. Follow-up: thread target into the full-file modal; persist last target. |
| 009  | "Mark file as viewed" review state | P2 | M | — | DONE (2026-06-17) — `useReviewedFiles` (localStorage-persisted); GitHub-style "Viewed" checkbox per file (auto-collapses), sidebar "N viewed" progress. Added test-env `localStorage` polyfill (`src/test/setup.ts`). Tests: `useReviewedFiles` unit, first `FileDiff` component test, e2e journey (mark→collapse→persist). |
| 010  | Sidebar file filter + reliability/test cleanups | P2 | S | — | DONE (2026-06-17) — `filterFiles` util + "Filter files…" input (filters sidebar list/tree + All-Files view, "X of N" count, empty state); `FullFileModal` fetch now uses `AbortController` (no stale state on close); tests: `filterFiles` + `prism` (`getLanguageFromFilename`/escape) units, e2e filter journey. Web: 56→64 unit tests, e2e 6→7. |
| 011  | Diff-target completion (persist + full-file honoring) | P2 | S | 008 | DONE (2026-06-17) — `GetFileDiffForTarget`/`GetFileDiffWithFullContextForTarget` + validated `target` on `/api/diff/{file}` and `/full`; `FullFileModal` threads `target`; selected target persists in localStorage with self-heal for deleted refs. Tests: backend target-file-diff, `FileList` component test (first), e2e target-persistence journey. Web: 64→66 unit, e2e 7→8. |
| 012  | Fix FileDiff status type bug + status badges/filter + watcher tests | P1 | M | — | DONE (2026-06-17) — fixed latent bug: TS `FileDiff` declared `isNew/isModified/...` booleans the API never sends (broke "renamed from" display); replaced with `status` matching the API. Added `StatusBadge` per file + status filter (`filterByStatus`). Made `watcher` testable (injectable status fn + pure `detectChangeType`) — last untested Go package now covered. Web: 66→71 unit, e2e 8→9; Go: watcher 0→42.9%. |
| 013  | Keyboard accessibility pass (WCAG 2.1.1) | P1 | M | — | DONE (2026-06-17) — converted clickable `<div>` file/folder rows in `FileList` to `<button>`s (with `aria-current` on the selected file, `aria-expanded` on folders); the `FileDiff` collapse chevron is now a focusable `<button aria-expanded>`; `FullFileModal` gained `role="dialog"`/`aria-modal`/`aria-labelledby`; focus-visible rings throughout. Tests: `FileList` role/aria assertions, e2e keyboard-collapse journey. Web: 71→73 unit, e2e 9→10. |
| 014  | Root error boundary + finish component coverage | P1 | S | — | DONE (2026-06-17) — added `ErrorBoundary` (class) wrapping `<App>` in `main.tsx`: a render crash now shows a recoverable fallback (message + reload) instead of a blank page. Tests: `ErrorBoundary` (children pass-through + throwing-child fallback) and first `DiffLine` tests (unified added line + split context line). Web: 73→77 unit (17 files). |
| 015  | Handler test coverage + full-file modal focus mgmt | P2 | S | — | DONE (2026-06-17) — handler tests for file endpoints (GetFileContent success/missing-path, GetFileDiff via router + unknown-target reject, GetFullFileWithDiff, GetComments file filter) raised handlers coverage 27.5%→41.5%. `FullFileModal` now moves focus into the dialog on open and restores it to the trigger on close. e2e: full-file modal open→Escape→focus-restored journey. Web e2e 10→11. |
| 016  | WebSocket hub tests + dark-mode/localStorage tests + filter-aware Collapse All | P2 | S | — | DONE (2026-06-17) — tested the WS hub (register/broadcast/unregister, shutdown stops Run, NotifyChange encoding) under -race → handlers 41.5%→50.7% (all 4 Go pkgs now meaningfully covered). Added `DarkModeToggle` + `useLocalStorage` unit tests (test-env `matchMedia` stub). "Collapse All/Expand All" now operates on the filtered/visible files instead of all files. e2e: dark-mode toggle + persistence. Web: 77→84 unit (19 files), e2e 11→12. |
| 017  | Edit review comments (close CRUD gap) | P2 | M | — | DONE (2026-06-17) — added comment editing (was add/delete only): `Store.UpdateComment`, `PUT /api/review/comment/{id}` (validated, size-limited), `useComments.updateComment`, inline editor in `CommentDisplay` (pencil button → textarea + Save/Cancel) threaded through `FileDiff`/`FullFileModal`. Tests: store+handler (200/400/404), `useComments.updateComment` (success/failure), `CommentDisplay` edit/cancel, e2e add→edit→persist. Web: 84→89 unit, e2e 12→13; review pkg 96.6%→97.1%, handlers →53.4%. |
| 018  | In-app "Copy review" (the AI export workflow) | P1 | S | — | DONE (2026-06-17) — the product's core AI workflow (export comments to paste into an assistant) was terminal-only; added a `CopyReviewButton` in the header that copies all comments as text (`file:line` + body, matching the terminal format) with a "Copied!" confirmation. Pure `formatCommentsAsText` util. Tests: util (5 cases incl. range/lineEnd-0/multi), component (count, clipboard write, confirmation), e2e (add→copy→assert clipboard contents). Web: 89→97 unit (23 files), e2e 13→14. |
| 019  | Copy review as JSON + failure handling | P2 | S | 018 | DONE (2026-06-17) — completes the dual-format export (CLI has `-format text\|json`): `CopyReviewButton` is now a text/JSON button group; `formatCommentsAsJSON` util; clipboard failures surface "Copy failed" instead of failing silently. Tests: `formatCommentsAsJSON` (round-trip, empty), component (JSON copy, failure state), e2e (copy text + JSON, parse clipboard JSON). Web: 97→101 unit. |
| 020  | Per-file comment-count badges in the sidebar | P2 | S | — | DONE (2026-06-17) — files with review comments now show an "N comments" badge (Solar chat icon, accessible label) in the sidebar list/tree, so reviewers can see at a glance where their annotations are. Pure `countCommentsByFile` util threaded via `commentCounts` prop. Tests: util (empty/grouped), `FileList` badge present/absent, e2e (add comment → sidebar badge appears). Web: 101→105 unit (26 files). |
| 021  | Testable router (cover main.go wiring) | P2 | S | — | DONE (2026-06-17) — extracted `newRouter(handler, wsHub, distFS)` from `main()` (catch-all reads `index.html` from the passed FS, dropping the dead dev fallback). Added `main_test.go` integration tests over the full router (SPA catch-all serves index.html for `/` and client routes, `/assets/*` static serving, `/api/diff`, comment POST→GET wiring) with an `fstest.MapFS`. `main` pkg 0→19.4% (5 Go pkgs now tested); e2e re-verified against the rebuilt binary. |
| 022  | Inline-edit keyboard shortcuts + README accuracy | P2 | S | — | DONE (2026-06-17) — the inline comment editor now matches the add dialog: Esc cancels, Cmd/Ctrl+Enter saves. README rewritten to document the actual feature set (compare-target, mark-viewed, filters, status badges, edit comments, copy text/JSON, comment-count badges, a11y). Hardened root `.gitignore` against stray Playwright artifacts. Tests: `CommentDisplay` Ctrl+Enter-saves + Esc-cancels; edit e2e now saves via Ctrl+Enter. Web: 105→107 unit. |
| 023  | Clipboard fallback for insecure contexts | P2 | S | 018 | DONE (2026-06-17) — "Copy review" failed when served over http on a non-localhost host (`navigator.clipboard` undefined). Added a `copyText` util that prefers the async Clipboard API and falls back to a hidden-textarea + `execCommand` path, so copy works in insecure contexts too; `CopyReviewButton` now uses it. Added test-env `document.execCommand` stub. Tests: `copyText` (API path, fallback when unavailable, fallback when API rejects, both-fail). Web: 107→111 unit (28 files). |
| 024  | Diff-stats summary + Go lint (golangci-lint) pass | P2 | S | — | DONE (2026-06-17) — added a sidebar "+X −Y" total diff-stats summary (pure `sumDiffStats` util, accessible label). Ran the project's real linter (`golangci-lint` v2, gosec/staticcheck/prealloc/gofmt — CI's Go lint job) for the first time: fixed 5 findings it surfaced (gofmt formatting in `service.go`/`main.go`/`watcher_test.go`, prealloc in `GetRefs` + `getUntrackedFileDiff`) → **0 issues**, de-risking CI. Tests: `sumDiffStats` (empty/totals), e2e stats-summary assertion. Web: 111→113 unit, e2e +1 assertion. |
| 025  | Automated a11y scan (axe-core) + fix contrast failures | P1 | S | — | DONE (2026-06-17) — added `@axe-core/playwright` e2e scans (main view + comment dialog, WCAG 2 A/AA, fail on serious/critical). Caught **real** contrast failures: the `+N`/`−N` diff-stat colors `#28a745`/`#d73a49` were 3.0–4.4:1 on light surfaces (below AA 4.5); switched to the accessible `#1a7f37`/`#cf222e` shades (matching `StatusBadge`). Scan settles past the transient "Updating…" state to avoid flakiness. `npm audit` re-checked → 0 vulns. e2e 14→16. |
| 026  | a11y coverage: dark mode + rich review state; comment-action discoverability | P2 | S | 025 | DONE (2026-06-17) — extended axe scans to **dark mode** and an **all-files + comment** state (exercising StatusBadge/CommentDisplay/badges) — both pass, verifying dark-mode contrast for the first time. UX: bumped the edit/delete comment icon buttons from .3/.4 → .55/.65 resting opacity so the actions are discoverable (still subtle, still brighten on hover). e2e 16→18. |
| 027  | Bound git command execution with timeouts | P2 | S | — | DONE (2026-06-17) — git invocations had no timeout, so a hung/slow command blocked the request indefinitely (UI spins forever). `git.Service.runGitCommand` now uses `exec.CommandContext` with a 30s timeout (`SetCommandTimeout` for tests) → a stuck git surfaces as a clean error/error-state instead of an infinite spinner. Same fix for the watcher's `git status` poll (10s) so a hang can't permanently stop live updates. Tests: `Service.CommandTimeout` (1ns→error, normal→ok). golangci-lint (gosec) clean. |
| 028  | Surface live-update connection status | P2 | S | — | DONE (2026-06-18) — when the WebSocket dropped (server restart/network blip) the app silently stopped live-updating with no indication. `useWebSocket` now reports status via an `onStatusChange` callback; `WebSocketContext` exposes `connected`; a `ConnectionStatus` indicator shows "Live updates paused — reconnecting…" only when disconnected (zero noise normally, `role="status"`). Tests: `useWebSocket` status (open→true/close→false), `ConnectionStatus` (hidden/shown), and a real e2e that kills the server and asserts the indicator appears (fixture now exposes `server.stop()`). Web: 113→116 unit, e2e 18→19. |
| 029  | Fix silent failure when saving a comment fails | P1 | S | — | DONE (2026-06-18) — `CommentDialog` cleared the textarea on submit and only `console.error`'d on failure → a failed add left an **empty dialog, no feedback, comment lost**. Now it submits async (Saving… state), closes only on success, and on failure shows a `role="alert"` error while **preserving the typed content** (so a retry after reconnect works). Tests: `CommentDialog` success-closes + failure-keeps-content-and-errors; e2e kills the server mid-compose and asserts the error + retained content. Fixture `stop()` now SIGKILLs (immediate drop, past graceful shutdown). Web: 116→117 unit, e2e 19→20. |
| 030  | Fix silent failure when saving a comment EDIT fails | P1 | S | 029 | DONE (2026-06-18) — same class of bug in the inline editor (`CommentDisplay.saveEdit` closed the editor immediately, reverting to old text and losing the edit on failure). Now it saves async (Saving… state), closes only on success, and on failure keeps the editor open with a `role="alert"` error and the **draft preserved**. Tests: `CommentDisplay` edit closes-on-success + keeps-draft-and-errors-on-failure; e2e adds a comment, edits it, kills the server, and asserts the error + retained draft. Web: 117→118 unit, e2e 20→21. |
| 031  | Surface comment DELETE failures (completes graceful-write trio) | P2 | S | 029, 030 | DONE (2026-06-18) — deleting a comment swallowed rejections (`void onDeleteComment(id)`) → a failed delete gave no feedback. `CommentDisplay` now awaits the delete and, on failure, shows a per-comment `role="alert"` ("Could not delete this comment. Try again.") while the comment remains; the 6 `FileDiff` sites pass the delete promise through. Tests: `CommentDisplay` delete-failure error; e2e adds a comment, kills the server, deletes, asserts the error + comment still present. Add/edit/delete now all fail gracefully. Web: 118→119 unit, e2e 21→22. |
| 032  | Make DiffLine memoization effective (large-diff perf) | P2 | S | — | DONE (2026-06-18) — `DiffLine` is `React.memo` but `FileDiff` passed fresh inline arrows (`onMouseEnter`/`onDragStart`) every render, defeating it → every line re-rendered on any parent update. Now `DiffLine` takes a `lineNumber` and binds the already-stable `useRangeSelection` handlers itself; `FileDiff` passes the stable handlers directly (dropped the unused `onMouseLeave` noop). So a selection/comment change only re-renders the affected lines, not the whole diff. Tests: `DiffLine` binding (onDragStart called with the line number) + existing render tests; all 22 e2e (add/edit/delete-by-drag) still pass. Web: 119→120 unit. |
| 033  | Respect prefers-reduced-motion | P2 | S | — | DONE (2026-06-18) — no global reduced-motion guard; pulsing indicators ("Updating…", the connection dot), hover transforms, and smooth scrolling ran at full motion for vestibular-sensitive users. Added the standard `@media (prefers-reduced-motion: reduce)` block in `index.css` neutralizing animation/transition durations and scroll-behavior app-wide. e2e: emulate `reducedMotion: 'reduce'` and assert a transition-heavy button's computed `transition-duration` is ≤1ms. e2e 22→23. |
| 034  | Fix drag-to-select range in split view | P2 | S | 032 | DONE (2026-06-18) — multi-line range commenting worked in unified view but not split: `DiffLine`'s split branch never attached `onMouseEnter` to its code cells, so drag-to-extend never fired (the stable handler was already passed in but unused). Attached `onMouseEnter={handleMouseEnter}` to both split code cells, so hovering a cell mid-drag extends the selection on the correct side. Tests: `DiffLine` split-cell `onMouseEnter` fires with the line number; first e2e covering split-view (add a comment in split mode). Web: 120→121 unit, e2e 23→24. |
| 035  | Robust diff parsing vs git config + fix "Updating…" contrast | P1 | S | — | DONE (2026-06-18) — **(a)** users with `diff.noprefix=true` / `diff.mnemonicPrefix=true` got prefix-less `diff --git` output the parser couldn't read → **no files shown at all**. Force `--src-prefix=a/ --dst-prefix=b/` on every diff so output is config-independent (Go test configures a no-prefix repo and asserts the diff still parses). **(b)** the axe a11y scan was intermittently failing — root cause was a **real** contrast bug: the pulsing "Updating…" indicator (`text-gray-400 animate-pulse`) dipped to 4.37:1 (below AA) at the pulse trough; switched to a static `text-gray-300`, fixing both the contrast and the flake (full suite green ×3). Go test +1. |
| 036  | Support non-ASCII / special-char filenames | P1 | S | — | DONE (2026-06-18) — with the default `core.quotePath=true`, git escapes/quotes non-ASCII paths (`"a/caf\303\251.txt"`), which the parser and status/ls-files handling can't read → **international filenames silently vanished from the diff**. Added `-c core.quotePath=false` globally in `runGitCommand` so git emits literal UTF-8 everywhere. Tests: Go (`café.txt` modified + `日本語.md` untracked both appear in the diff); e2e (refactored fixtures into a reusable `launchServer`; new `unicodeURL` fixture + journey rendering `café日本語.txt` through the real binary). Go test +1, e2e 24→25. |
| 037  | Don't render untracked binary files as garbage | P2 | S | — | DONE (2026-06-18) — untracked binaries bypassed git's binary detection: `getUntrackedFileDiff` read the bytes and split them into "lines" → garbage rows in the UI. Added `isBinaryContent` (git's NUL-byte-in-first-8000 heuristic); untracked binaries now return `IsBinary` with no hunks. Frontend: added `isBinary` to the TS type and a "Binary file — content not shown." message in `FileDiff` (covers tracked + untracked binaries). Tests: Go (`isBinaryContent`; untracked `.png` flagged binary, no hunks), `FileDiff` binary-message unit test, e2e (`binaryURL` fixture → asserts the message). Go tests +2, web 121→122 unit, e2e 25→26. |
| 038  | Fix untracked-file line splitting (trailing blank / empty) | P2 | S | — | DONE (2026-06-18) — `getUntrackedFileDiff` did `strings.Split(content, "\n")`, so a file ending in `\n` produced a **spurious trailing blank line and an off-by-one additions count**; empty files produced a phantom `@@ -0,0 +1,0 @@` hunk. Now trims the single trailing newline and emits no hunk for empty files. Tests: Go (3-line file → exactly 3 added lines with correct content; empty file → 0 additions/0 hunks); e2e (`newFileURL` fixture asserts exactly 3 `.diff-line` rows, not 4). Go tests +2, e2e 26→27. |
| 039  | Support repos with no commits yet | P1 | S | — | DONE (2026-06-18) — in a freshly `git init`'d repo (no HEAD), the default "All" view ran `git diff HEAD` which **fatals** → a user reviewing their very first staged files saw "Couldn't load the diff". Added `hasCommits()`; when there's no HEAD, the "All" view falls back to `git diff --cached` so staged files show (untracked already handled). Tests: Go (no-commit repo: `hasCommits()` false, staged `first.txt` appears, no error); e2e (`noCommitURL` fixture → staged file + content render, no error state). Go test +1, e2e 27→28. |
| 040  | Fail fast (clear message) when not in a git repo | P2 | S | — | DONE (2026-06-18) — running `vibediff` outside a git repo started a server that could produce nothing, and auto-opened a browser to a generic error. Added `IsGitRepo()` (`git rev-parse --is-inside-work-tree`); `main` now prints "Error: not a git repository. Run vibediff inside a git repository." and exits 1 **before** starting the server/opening the browser. Tests: Go `IsGitRepo` (true in repo, false in a non-git dir); verified the built binary exits 1 with the message in a non-git dir; the 28 e2e (all git-repo fixtures) confirm the positive path still starts. Go test +1. |
| 041  | Focus-trap the full-file modal (+ DRY the trap) | P2 | S | — | DONE (2026-06-18) — `FullFileModal` didn't trap Tab focus (it could escape to the obscured page behind it — a WCAG modal violation). Extracted a reusable `useFocusTrap(ref, active)` hook; applied it to `FullFileModal` (new) and refactored `CommentDialog` to use it (removing its bespoke handler — DRY). Tests: `useFocusTrap` unit (Tab→first, Shift+Tab→last, inactive no-op); e2e asserts focus stays within the full-file dialog after Shift+Tab. Web: 122→125 unit. |
| 042  | Test the live-update pipeline (WS handler + watcher→UI) | P1 | S | — | DONE (2026-06-18) — the product's headline "live file watching" was untested end-to-end, and the WS handler/pumps were uncovered. Added: Go integration test connecting a real `websocket` client to `HandleWebSocket` (upgrade → "connected" message → broadcast received), raising handlers coverage 53.4%→67.7% (passes under -race); and an e2e that edits a file on disk (`server.writeFile`) and asserts the diff refreshes (watcher→WebSocket→refetch) with no reload. e2e 28→29 (stable ×2). |
| 043  | "/" shortcut to focus file filter + remove dead code | P3 | S | — | DONE (2026-06-18) — added a `/` keyboard shortcut (common search pattern) to jump to the file filter (placeholder hints "press /"); ignored while typing in inputs. Removed dead `Service.GetStatus` (unused in production; repointed the timeout test to `GetRefs`). Tests: e2e (`/` focuses the empty filter without typing into it). Web e2e 29→30. |
| 044  | PrismThemeManager test + cleanup; Escape clears filter | P3 | S | — | DONE (2026-06-18) — covered the last untested component: `PrismThemeManager` (injects light theme by default, swaps to dark on `.dark`, and — newly — removes its `<link>` on unmount, fixing a small leak). UX: Escape in the file filter clears it and blurs (completes the `/`→filter→Escape flow). Tests: 3 `PrismThemeManager` units; e2e (Escape clears the filter and restores the file list). Web 125→128 unit, e2e 30→31. |
| 045  | Fix latent CI failure: golangci-lint v2 action mismatch | P2 | S | — | DONE (2026-06-18) — the backend CI lint job would have failed on every run: `.golangci.yml` is `version: "2"` but CI pinned `golangci-lint-action@v6`, which only supports golangci-lint v1. Bumped to `@v7` (the version that understands v2 config) and pinned the binary to `v2.1.6` (matches local; reproducible over floating `latest`). Also aligned both jobs' Node to 22 (current LTS / Vite 8 baseline). Verified locally by running the exact CI gates: `go vet`, `go test -race ./...`, and `golangci-lint run` (v2.1.6) → 0 issues; ci.yml validated as YAML. This is the safety net for the pushed branch and PRs. |
| 046  | Word-level (intra-line) diff highlighting | P1 | M | — | DONE (2026-06-18) — the long-deferred flagship feature. New `web/src/utils/wordDiff.ts`: token-level LCS diff between each paired deletion/addition line → changed character ranges, overlaid onto the existing Prism syntax HTML by a **tag-aware range wrapper** (`wrapRanges`) that closes/reopens the highlight at every Prism `<span>` boundary, so word highlights and syntax highlighting coexist with valid markup. Unicode-aware tokenizer; pairs delete/add blocks index-for-index per hunk; guards (no common content → skip; >400 tokens → skip minified lines). `FileDiff` computes ranges once per file (stable refs preserve `DiffLine` memo); `DiffLine` overlays them; new `.diff-word-add`/`.diff-word-del` GitHub-style CSS (light + dark). Tests: 17 `wordDiff` units (tokenize, LCS ranges, merge, pairing, tag/entity-boundary wrapping) + e2e (only the changed word is highlighted, not the whole line). Web 128→145 unit, e2e 31→32. All gates green. |
| 047  | Keyboard-shortcuts help overlay (discoverability) | P2 | S | — | DONE (2026-06-18) — the app had `j`/`k` nav, `/` filter-focus, and collapse-all but **no way to discover them**. New accessible `KeyboardShortcutsDialog` (role=dialog, aria-modal, focus trap via existing `useFocusTrap`, focus restore, Escape + backdrop close, `<kbd>`-styled keys) opened with `?` or a header keyboard-icon button (`aria-keyshortcuts="?"`). Handler matches both `key === "?"` and Shift+`/` (env-dependent), guards the plain-`/` shortcut against Shift, and freezes background nav while the overlay is open. Tests: 5 dialog units (closed renders nothing; accessible roles; Close/Escape/backdrop behavior) + 2 e2e (open via `?` and header button → Escape/Close; axe a11y scan of the dialog → 0 serious). Web 145→150 unit, e2e 32→34. All gates green. |
| 048  | Fix: comments on deleted lines + full shadcn/ui migration | P1 | L | — | DONE (2026-06-18) — **Bug:** commenting on a deleted line failed ("Could not save your comment"). The client encodes old-side lines as negative line numbers, but `validateComment` rejected `Line < 1`. Now accepts any non-zero line (still rejects 0); terminal output prints magnitudes; Go test updated (negative → 200). **shadcn/ui full migration:** added foundation (clsx/tailwind-merge/cva + Radix primitives + `@/` alias + `cn` + components.json + `tw-animate-css`); semantic theme tokens (`:root`/`.dark` + `@theme inline`) mapped to the GitHub palette so the look is preserved. New `src/components/ui/`: button, dialog, input, badge, checkbox, tooltip. Migrated every interactive surface: StatusBadge→Badge, the 3 modals→Radix Dialog (focus trap/Escape/backdrop/focus-restore now via Radix; added trigger-capture focus restore to the shared DialogContent since dialogs are controlled), header toggle groups + Collapse/Wrap/keyboard/dark-mode/copy buttons→Button, file filter→Input, "Viewed"→Checkbox, edit/delete→ghost Buttons, keyboard button→Tooltip. Notable fixes: cleared CommentDialog draft on close (Radix exit-animation kept a stale textarea in the DOM), moved FullFileModal's nested CommentDialog to a sibling to avoid nested focus-scope conflicts. TargetSelector/status filter kept as native selects (accessible, no Radix Select dep added). Updated dialog unit tests for Radix (no `aria-modal` attr; modality via focus trap). All gates green: web tsc/eslint/vitest **150**/build, e2e **34**, go vet/test -race/golangci-lint **0**. Bundle 379→475 KB (gzip 118→153) from Radix. |
| 049  | Complete shadcn migration: native selects → shadcn Select | P2 | M | — | DONE (2026-06-18) — finished the component migration by replacing the two remaining native `<select>`s (TargetSelector in the dark header + the sidebar status filter) with a new `src/components/ui/select.tsx` (Radix Select: themed trigger, grouped items, check indicator, keyboard nav, portal/animation, `role=combobox`). TargetSelector now groups refs under "Branches"/"Tags" labels; maps the default option to a non-empty sentinel (Radix reserves `""` for the placeholder). Tests: rewrote `TargetSelector` units to assert the closed trigger's value→label mapping (Radix Select's portal interaction needs a real browser, so opening/selecting is covered by e2e); updated the 2 affected e2e flows from native `selectOption`/`toHaveValue` to click-trigger→click-option/`toContainText`; added a **net-new e2e** verifying the dropdown renders grouped Branches/Tags + Escape-closes. New `src/lib/utils.test.ts` locks the `cn`/tailwind-merge conflict-resolution behavior the ui layer depends on. Web 150→153 unit, e2e 34→35. All gates green (tsc/eslint/vitest/build, go vet/test/golangci-lint 0). |
| 050  | Toast notifications (sonner) for clipboard feedback | P2 | S | — | DONE (2026-06-18) — replaced the hacky label-swap feedback on the copy-review buttons (which briefly rewrote the button text to "Copied!"/"Copy failed", shifting layout) with proper **sonner toasts**. New `src/components/ui/sonner.tsx` Toaster mounted at the app root, themed via our own CSS tokens (`--popover`/`--border`) so it tracks the manual dark toggle automatically and respects reduced motion. `CopyReviewButton` simplified: dropped the `useState`/`useRef`/`useEffect`/`setTimeout` label-swap state machine — now stable labels + `toast.success`/`toast.error` ("Review copied as text/JSON", "Could not copy to clipboard"). Tests: reworked `CopyReviewButton` units to mock `sonner` and assert toast calls; updated the copy e2e to assert the toast text instead of the button label. Web e2e 35 (updated). All gates green; bundle +~10 KB (sonner). |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (reason) | REJECTED (rationale)

## Dependency notes

- **001 floats to the top**: it's the verification baseline. Every other plan's
  done-criteria assume CI exists to run them on PRs. 001 itself needs no other
  plan and is safe (additive).
- **003 and 004 should land before or alongside risky changes** (005, 006):
  they establish the regression net that proves those fixes don't break behavior.
- **002 before 006**: 006's hardening assumes the path-validation helper from
  002 exists (it reuses it for the comment `file` field).
- 005, 006, 007 are independent of each other and can run in any order once
  their deps are met.

## Findings considered and rejected

- **WebSocket hub data race**: NOT a race — `WSHub.clients` map is only ever
  touched inside the single `Run()` goroutine via channels (`websocket.go:48-89`).
  Safe by design.
- **Review store data race**: NOT a race — `Store` guards `comments` with
  `sync.RWMutex` (`store.go:20-73`). Safe.
- **`diffTarget` command injection** (`service.go:34`): the target comes from a
  CLI positional arg controlled by the operator running the binary, never from
  an HTTP request. `exec.Command` uses arg-slice (no shell). Defense-in-depth
  `--` separator is folded into plan 002 as a minor step, not a standalone bug.
- **Double-close of WebSocket conn on shutdown** (`websocket.go:79`): gorilla's
  `Conn.Close()` returns an error on second call rather than panicking; no crash.
  Cosmetic; not worth a dedicated plan (the redundant-Close can be cleaned up
  opportunistically during plan 005).
- **Virtual scrolling for large diffs** (PERFORMANCE-02): real, but L-effort
  architectural change that only matters for very large (5k+ line) diffs. Left as
  a direction option for the maintainer, not a near-term plan.
</content>
</invoke>
